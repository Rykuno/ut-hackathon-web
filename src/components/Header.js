import React from 'react';
import hackathonBanner from '../images/hackathonBanner.gif';

const Header = () => (
  <div>
    <img className="banner-gif" src={hackathonBanner} alt="#" />
  </div>
);

export default Header;
