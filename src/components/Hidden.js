import React from 'react';
import { Button } from 'react-bootstrap';
import image from '../images/tryToOutguessThis.jpg';

const Hidden = () => (
  <div>
    <img src={image} alt="#" />
  </div>
);

export default Hidden;
